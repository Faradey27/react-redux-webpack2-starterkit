import { expect } from 'chai';

describe('apps action tests', () => {
  describe('test constants', () => {
    it('verify existence of LOAD_EXPLORE_GRAPH', () => {
      expect(true).to.eql(true);
    });
  });
});
